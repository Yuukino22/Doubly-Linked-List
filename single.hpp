#ifndef SINGLE_HPP
#define SINGLE_HPP

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <stdio.h>
#include "Queue.hpp"

//The main function of single line.
void singleLine(int simulationTime, int registers, int maxLineLength, std::vector<int> registerTime, std::vector<std::pair<int, int>> customerArrival);


#endif